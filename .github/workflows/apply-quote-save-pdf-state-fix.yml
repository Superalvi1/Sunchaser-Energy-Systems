name: Apply quote save and PDF state hotfix

on:
  push:
    branches:
      - fix/quote-save-pdf-state
    paths:
      - .github/workflows/apply-quote-save-pdf-state-fix.yml

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout hotfix branch
        uses: actions/checkout@v4
        with:
          ref: fix/quote-save-pdf-state
          fetch-depth: 0

      - name: Apply focused source fixes
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          sales_path = Path('src/components/SalesTeamApp.tsx')
          sales = sales_path.read_text()

          def replace_once(src: str, old: str, new: str, label: str) -> str:
              count = src.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected 1 match, found {count}')
              return src.replace(old, new, 1)

          sales = replace_once(
              sales,
              '  const [savingQuote, setSavingQuote] = useState<boolean>(false);\n',
              '  const [savingQuote, setSavingQuote] = useState<boolean>(false);\n  const savingQuoteRef = useRef<boolean>(false);\n',
              'saving quote ref state',
          )

          sales = replace_once(
              sales,
              '  const handleSaveSizerQuote = async () => {\n    if (!activeLead || savingQuote) return;\n',
              '  const handleSaveSizerQuote = async () => {\n    if (!activeLead || savingQuoteRef.current) return;\n',
              'sizer duplicate-save guard',
          )

          sales = replace_once(
              sales,
              '    setSubmitError(null);\n    setSavingQuote(true);\n    try {\n      const sizerKw = systemSizekW;\n',
              '    setSubmitError(null);\n    savingQuoteRef.current = true;\n    setSavingQuote(true);\n    try {\n      const sizerKw = systemSizekW;\n',
              'sizer saving lock',
          )

          sales = replace_once(
              sales,
              '    if (savingQuote) return null;\n    if (activeModule === "sizer") {\n',
              '    if (savingQuoteRef.current) return null;\n    if (activeModule === "sizer") {\n',
              'manual duplicate-save guard',
          )

          sales = replace_once(
              sales,
              '    setSavingQuote(true);\n\n    try {\n      const panelsCount = Math.ceil((systemSizekW * 1000) / panelWattage);\n',
              '    savingQuoteRef.current = true;\n    setSavingQuote(true);\n\n    try {\n      const panelsCount = Math.ceil((systemSizekW * 1000) / panelWattage);\n',
              'manual saving lock',
          )

          refresh_old = '      if (onRefreshState) await onRefreshState();\n'
          refresh_new = '''      if (onRefreshState) {\n        void Promise.resolve()\n          .then(() => onRefreshState())\n          .catch((refreshErr) => console.error("Quote refresh failed:", refreshErr));\n      }\n'''
          if sales.count(refresh_old) != 2:
              raise SystemExit(f'background refresh: expected 2 matches, found {sales.count(refresh_old)}')
          sales = sales.replace(refresh_old, refresh_new)

          finally_old = '''    } finally {\n      setSavingQuote(false);\n    }\n'''
          finally_new = '''    } finally {\n      savingQuoteRef.current = false;\n      setSavingQuote(false);\n    }\n'''
          if sales.count(finally_old) != 2:
              raise SystemExit(f'saving lock cleanup: expected 2 matches, found {sales.count(finally_old)}')
          sales = sales.replace(finally_old, finally_new)

          resolver_old = '''  const resolveTargetManualQuote = () => {\n    if (!activeLead) return null;\n    return editingQuoteId\n      ? activeLead.quotes?.find((q: any) => q.id === editingQuoteId && q.quote_type === "manual_boq")\n      : getLatestSavedQuote(activeLead, "manual_boq");\n  };\n'''
          resolver_new = '''  const resolveTargetManualQuote = () => {\n    if (!activeLead) return null;\n    if (editingQuoteId) {\n      const refreshedQuote = activeLead.quotes?.find(\n        (q: any) => q.id === editingQuoteId && q.quote_type === "manual_boq"\n      );\n      // The create API returns the quote id before the parent lead refresh finishes.\n      // Keep Print/Download usable during that short window instead of pretending nothing was saved.\n      return refreshedQuote || { id: editingQuoteId, quote_type: "manual_boq" };\n    }\n    return getLatestSavedQuote(activeLead, "manual_boq");\n  };\n'''
          sales = replace_once(sales, resolver_old, resolver_new, 'saved-id resolver fallback')

          manual_print_old = '''    if (!targetManualQuote) {\n      alert("Save a quote first.");\n      return;\n    }\n    try {\n      await openManualQuotePrintPreview(activeLead.id, targetManualQuote.id);\n'''
          manual_print_new = '''    if (!targetManualQuote) {\n      alert("Please save the customer quotation before generating the PDF.");\n      return;\n    }\n    try {\n      await openManualQuotePrintPreview(activeLead.id, targetManualQuote.id);\n'''
          sales = replace_once(sales, manual_print_old, manual_print_new, 'manual print friendly save message')

          sales_path.write_text(sales)

          export_path = Path('src/lib/quotePdfExport.ts')
          export_src = export_path.read_text()
          friendly_old = '''function friendlyPdfError(status: number, text: string): string {\n  const trimmed = (text || "").trim();\n  if (trimmed.includes("PDF engine is not installed")) return PDF_ENGINE_MISSING_MESSAGE;\n  if (/executable doesn't exist|playwright install/i.test(trimmed)) return PDF_ENGINE_MISSING_MESSAGE;\n  return trimmed || `PDF download failed (${status})`;\n}\n'''
          friendly_new = '''function friendlyPdfError(status: number, text: string): string {\n  const trimmed = (text || "").trim();\n  if (trimmed.includes("PDF engine is not installed")) return PDF_ENGINE_MISSING_MESSAGE;\n  if (/executable doesn't exist|playwright install/i.test(trimmed)) return PDF_ENGINE_MISSING_MESSAGE;\n\n  let message = trimmed;\n  if (trimmed.startsWith("{")) {\n    try {\n      const parsed = JSON.parse(trimmed) as { error?: unknown; message?: unknown };\n      if (typeof parsed.error === "string") message = parsed.error;\n      else if (typeof parsed.message === "string") message = parsed.message;\n    } catch {\n      // Keep the original response text when it is not valid JSON.\n    }\n  }\n\n  if (/save a quote first/i.test(message)) {\n    return "Please save the customer quotation before generating the PDF.";\n  }\n  return message || `PDF download failed (${status})`;\n}\n'''
          export_src = replace_once(export_src, friendly_old, friendly_new, 'friendly JSON PDF error parsing')
          export_path.write_text(export_src)

          test_path = Path('src/lib/quotePdfExport.test.ts')
          tests = test_path.read_text()

          marker = '''check("manual PDF URLs remain on the manual-quote routes", () => {\n  assert.match(manualQuotePdfPreviewUrl("lead-1", "q-m"), /\\/api\\/export\\/pdf\\/manual-quote\\/lead-1/);\n  assert.match(manualQuotePdfDownloadUrl("lead-1", "q-m"), /\\/api\\/export\\/pdf\\/manual-quote\\/lead-1\\/download/);\n  assert.doesNotMatch(manualQuotePdfDownloadUrl("lead-1", "q-m"), /auto-sizer/);\n});\n'''
          addition = marker + '''\ncheck("PDF API JSON errors are converted to a customer-friendly save message", () => {\n  const friendly = sliceBetween(\n    exportSrc,\n    "function friendlyPdfError",\n    "export function manualQuotePdfPreviewUrl"\n  );\n  assert.match(friendly, /JSON\\.parse\\(trimmed\\)/);\n  assert.match(friendly, /Please save the customer quotation before generating the PDF\\./);\n});\n'''
          tests = replace_once(tests, marker, addition, 'friendly PDF error regression test')

          generated_marker = '''check("Generated Quotes routes AutoSizer versions to AutoSizer helpers", () => {\n'''
          state_test = '''check("manual customer save returns its id without waiting for the parent refresh", () => {\n  const saveFn = sliceBetween(\n    salesSrc,\n    "const handleSaveQuote = async",\n    "const resolveTargetManualQuote"\n  );\n  assert.match(saveFn, /setEditingQuoteId\\(savedQuoteId\\)/);\n  assert.match(saveFn, /Promise\\.resolve\\(\\)\\s*\\.then\\(\\(\\) => onRefreshState\\(\\)\\)/);\n  assert.doesNotMatch(saveFn, /await onRefreshState\\(\\)/);\n  assert.match(saveFn, /savingQuoteRef\\.current = false/);\n});\n\ncheck("recently saved manual quote id stays printable before lead refresh completes", () => {\n  const resolver = sliceBetween(\n    salesSrc,\n    "const resolveTargetManualQuote",\n    "const handleDownloadManualQuotePDF"\n  );\n  assert.match(resolver, /refreshedQuote \\|\\| \\{ id: editingQuoteId, quote_type: "manual_boq" \\}/);\n});\n\ncheck("manual PDF download uses the id returned directly by save", () => {\n  const downloadFn = sliceBetween(\n    salesSrc,\n    "const handleDownloadManualQuotePDF",\n    "const handlePrintManualQuotePDF"\n  );\n  assert.match(downloadFn, /const savedId = await handleSaveQuote\\(\\)/);\n  assert.match(downloadFn, /resolvedQuoteId = savedId/);\n});\n\n'''
          if generated_marker not in tests:
              raise SystemExit('missing Generated Quotes marker for state regression tests')
          tests = tests.replace(generated_marker, state_test + generated_marker, 1)
          test_path.write_text(tests)
          PY

      - name: Install dependencies
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: '1'
        run: npm ci --ignore-scripts

      - name: Run quotation regression tests
        env:
          PLAYWRIGHT_BROWSERS_PATH: '0'
        run: |
          npm run test:quote-pdf-export
          npm run test:quote-customer-boq
          npm run test:quote-commercial
          npx tsc --noEmit

      - name: Build production frontend
        run: npx vite build --mode production

      - name: Commit verified fix and remove patcher
        shell: bash
        run: |
          git diff --check
          git config user.name 'Sunchaser CI'
          git config user.email 'actions@users.noreply.github.com'
          git rm .github/workflows/apply-quote-save-pdf-state-fix.yml
          git add src/components/SalesTeamApp.tsx src/lib/quotePdfExport.ts src/lib/quotePdfExport.test.ts
          git commit -m 'fix: stabilize quote save and PDF actions'
          git push origin HEAD:fix/quote-save-pdf-state
